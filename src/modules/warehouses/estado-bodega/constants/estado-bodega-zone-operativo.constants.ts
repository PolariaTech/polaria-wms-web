/** Tareas con más de este tiempo generan alerta de demora (siguen visibles en pendientes). */
export const TAREA_DEMORA_ALERTA_MS = 5 * 60 * 1000;

/** Por encima de este valor (°C) en zona ingreso se genera alerta de temperatura. */
export const TEMPERATURA_ALTA_UMBRAL_C = 5;
